import 'package:flutter/material.dart';

class Logolegend extends StatelessWidget {
  const Logolegend({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}